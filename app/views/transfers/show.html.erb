<p id="notice"><%= notice %></p>

<div class="card text-center">
  <div class="card-header">
    Transferência Realizada
  </div>
  <div class="card-body">
    <h5 class="card-title">Valor: <b><%= real(@transfer.amount) %></b></h5>
    <p class="card-text">De: <b><%= @transfer.account.name %> - <%= @transfer.account_id %></b></p>
    <p class="card-text">Para: <b><%= find_account(@transfer.account_to).name %> - <%= @transfer.account_to %></b></p>
    <%= link_to 'Voltar', transfers_path, class: 'btn btn-secondary' %>
  </div>
  <div class="card-footer text-muted">
    Criado em: <%= br_date(@transfer.created_at) %>
  </div>
</div>