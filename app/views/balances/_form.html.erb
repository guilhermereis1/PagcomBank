<%= form_with(model: balance) do |form| %>
  <% if balance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(balance.errors.count, "error") %> prohibited this balance from being saved:</h2>

      <ul>
        <% balance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.text_field :account_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :deposit_references %>
    <%= form.text_area :deposit_references %>
  </div>

  <div class="field">
    <%= form.label :withdraw_references %>
    <%= form.text_area :withdraw_references %>
  </div>

  <div class="field">
    <%= form.label :transfer_references %>
    <%= form.text_area :transfer_references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
