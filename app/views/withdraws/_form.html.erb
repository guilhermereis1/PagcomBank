<%= form_with(model: withdraw) do |form| %>
  <% if withdraw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(withdraw.errors.count, "error") %> prohibited this withdraw from being saved:</h2>

      <ul>
        <% withdraw.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.select(:account_id, @user_accounts, { include_blank: "Selecione uma Conta" }, { :class => 'form-control' } ) %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, as: :currency, class: 'form-control' %>
  </div>

  <div class="actions">
    <br>
    <%= form.submit class: 'btn btn-success' %>
  </div>
<% end %>
